import gradient from '../img/california-inverse.jpg'

const modal = ({
  background: `url("${gradient}")`,
  width: '100%',
  padding: '1rem 2em',
  color: 'white',
  overflow: 'hidden',
  display: 'flex',
  margin: 0,
});

export default modal;
